import React, {FC} from 'react';
import Header from '../Header';
import Footer from '../Footer';

const Layout:FC = () => {
  return (
    <div className="">
        <Header/>
        RenderBody
        <Footer/>
    </div>
  );
}

export default Layout;
